import Foundation

/// Defines the layout style for the tab bar
public enum TabLayoutStyle {

    /// Fills the device width evenly
    case fixed

    /// Adjusts dynamically to content size, scrollable
    case scrollable

}
